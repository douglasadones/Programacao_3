// elemento.id, elemento.name, elemento.value

//node.getAttribute("nomeDoAtributo") aqui ele retorna exatamente o que tem no atributo
// ou node.nomeDoAtributo Aqui ele pode retornar um objeto

// Todo Elemento é um nó, mas nem todo nó(Node) é um elemento.

// A DOM é composta por Nós(Nodes)

// Criando novo nó: node.appendChild(novoNó)

// Removendo um novo nó: node.removeChild(noASerRemovido);

// node.remove() o node se exclui e, com ele, todos os seus filhos

// comandos como node.cloneNode() ou document.createElement() não ficam vinculados na DOM, sendo necessário fazer o vínculo manualmente.

// Pesquisar mais sobre os parâmetros node.cloneNode()